import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // TODO: Add app-level smoke tests once core widgets are built
    expect(true, isTrue);
  });
}
